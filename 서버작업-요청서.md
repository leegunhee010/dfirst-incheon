# 서버 작업 요청서 — 퍼스트디자인 인천지사

작성일 2026-07-28 · 대상 저장소 `zhiwei-lan/dfirst-incheon`

---

## 0. 현재 상태

| 항목 | 상태 |
|---|---|
| 정적 사이트 19페이지 | **동작 중** — https://dfirst-incheon.vigo.co.kr |
| HTTPS | **적용 완료** (HTTP → HTTPS 리다이렉트도 동작) |
| GitHub Actions 자동배포 | **동작 중** (main 푸시 → 웹루트 `git pull`) |
| 관리자 백엔드(CI4) | **미설치** — `/api/me` 가 404 (화면은 열리나 로그인 불가) |
| 소스·데이터 공개 노출 | **차단 필요** (아래 3번) |

요청드리는 작업은 **① 보안 차단 ② 관리자 설치** 두 가지입니다.
①은 지금 바로, ②는 관리자 기능을 쓰기 시작할 때 필요합니다.

*(2026-07-28 08:20 기준 확인. HTTPS는 요청 전에 이미 적용해 주신 것으로 확인했습니다.)*

---

## 1. 서버 환경

- PHP **8.2** (CodeIgniter 4)
- MySQL **5.6**
- 웹서버 openresty(nginx) · 1Panel
- 웹루트 `/opt/1panel/www/sites/dfirst-incheon.vigo.co.kr/index`

> 패키지는 위 버전 기준으로 검증했습니다. MySQL 5.6에서 못 쓰는 문법
> (JSON 컬럼·생성 컬럼·표현식 DEFAULT·8.0 콜레이션)은 전부 배제했고,
> utf8mb4 인덱스 키가 767바이트를 넘지 않도록 컬럼 길이를 잡았습니다.

---

## 2. 사이트 구조 (참고)

```
웹루트/
├─ *.html            사이트 19페이지 — 웹서버가 그대로 서빙 (PHP 불필요)
├─ theme/assets/     이미지·CSS·폰트
├─ sitemap.xml, robots.txt, rss.xml
├─ admin/            관리자 화면 (index.html, admin.js, admin.css)
├─ ci4-admin/        ★관리자 백엔드 소스 — 설치용, 웹에 노출되면 안 됨
└─ admin/data/*.json ★로컬 작업 데이터 — 웹에 노출되면 안 됨
```

- **사이트 화면은 정적 HTML**이라 서버 부하가 없고 검색엔진이 소스를 그대로 읽습니다.
- **관리자에서 저장하면 정적 HTML 파일을 직접 수정**합니다. 그래서 웹루트에 쓰기 권한이 필요합니다.

---

## 3. ①보안 — 노출 경로 차단 (급함)

웹루트에서 `git pull` 하는 구조라 **저장소의 모든 파일이 외부에서 다운로드됩니다.**
2026-07-28 확인 결과 아래가 열려 있었습니다.

| 경로 | 조치 |
|---|---|
| `/ci4-admin/sql/seed.sql` | 관리자 계정 해시가 노출됐었음 → **파일에서 제거 완료** |
| `/ci4-admin/app/**.php` | PHP 미설치라 소스가 텍스트로 읽힘 → **차단 필요** |
| `/admin/data/*.json` | 사이트 데이터 → **차단 필요** |
| `/_*.py`, `*.md` | 빌드 스크립트·문서 → **차단 필요** |

1Panel → 웹사이트 → 설정 → 구성 파일에 추가해 주세요.

```nginx
location ~ ^/(ci4-admin|admin/data|_src)/ {
    deny all;
    return 404;
}

location ~ \.(py|sql|md)$ {
    deny all;
    return 404;
}
```

**확인** (둘 다 404여야 정상):

```bash
curl -o /dev/null -w "%{http_code}\n" https://dfirst-incheon.vigo.co.kr/ci4-admin/sql/schema.sql
curl -o /dev/null -w "%{http_code}\n" https://dfirst-incheon.vigo.co.kr/admin/data/settings.json
```

---

## 4. HTTPS — 완료 (조치 불필요)

확인 결과 이미 적용되어 있습니다. HTTP 접속도 HTTPS로 리다이렉트됩니다. 감사합니다.

---

## 5. ②관리자(CI4) 설치

### 5-1. 데이터베이스

```sql
CREATE DATABASE firstd_incheon DEFAULT CHARSET utf8mb4;
```

```bash
cd 웹루트/ci4-admin
mysql firstd_incheon < sql/schema.sql                  # 테이블 11개
mysql firstd_incheon < sql/seed.sql                     # 포트폴리오 450·히어로 등 기본 데이터
mysql firstd_incheon < sql/seed_data_2026-07-28.sql      # 칼럼 4글·카피 71·SEO 14 (최신분)
```

> 이미 예전 스키마로 설치한 서버라면 `schema.sql`을 다시 넣지 말고
> `mysql firstd_incheon < sql/migrate_2026-07-28.sql` 만 실행해 주세요.

### 5-2. 관리자 계정 생성

보안상 계정 정보를 SQL 파일에 넣지 않았습니다. 원하는 비밀번호로 직접 만들어 주세요.

```sql
SET @pw := '여기에_새_비밀번호';
INSERT INTO `admins`(username, password_hash, created_at)
  VALUES('admin', SHA2(@pw, 256), CURDATE());
```

### 5-3. CI4 앱 배치

- `ci4-admin/app/Controllers/Api.php` → CI4 프로젝트 `app/Controllers/`
- `ci4-admin/app/Libraries/Bake.php` → `app/Libraries/`
- `ci4-admin/app/Config/Routes.php` 의 라우트 블록을 기존 `Routes.php` 의 `$routes` 정의부에 **병합**
- `ci4-admin/app/Config/Database.sample.php` 를 참고해 실제 `Database.php` 의 `$default` 작성

### 5-4. 관리자 화면

`ci4-admin/public/admin/` 의 3파일(index.html, admin.js, admin.css)을 웹루트 `admin/` 에 배치해 주세요.
※ **이 패키지 버전의 admin.js를 사용**해 주세요 (카피 편집 경로가 `/editmode/` 로 맞춰져 있습니다).

### 5-5. 권한

관리자 저장 시 **정적 HTML을 직접 수정**하므로 웹서버 계정(www-data 등)에 쓰기 권한이 필요합니다.

```bash
chown -R www-data:www-data 웹루트
chmod -R 775 웹루트
```

대상: 웹루트의 `*.html`, `col-*.html`, `theme/assets/`, `sitemap.xml`, `rss.xml`

### 5-6. 확인

```bash
curl -o /dev/null -w "%{http_code}\n" http://도메인/api/me     # 200 이어야 정상 (현재 404)
```

브라우저에서 `도메인/admin` 접속 → 로그인 → 각 탭(칼럼·포트폴리오·카피·SEO) 동작 확인.

---

## 6. 도메인

최종 서비스 도메인은 **`incheondesign.co.kr`** 입니다.
현재 사이트의 canonical·구조화데이터가 이 주소 기준으로 작성되어 있습니다
(지금 `vigo.co.kr`로 떠 있어도 검색엔진은 위 주소를 정본으로 인식하므로 문제 없습니다).

도메인 연결 후 관리자 → SEO → 사이트 설정에서 도메인 값이 위와 같은지 확인해 주세요.

---

## 7. 선택 작업 — 문의 폼 DB 연동

현재 문의 폼은 **메일 발송(mailto)** 방식입니다. 관리자 "견적 문의" 탭에 쌓이게 하려면
폼 제출을 `POST /api/inquiries` 로 보내도록 연결해 주세요.

```json
{ "company": "", "name": "", "phone": "", "email": "", "field": "", "message": "" }
```

연결하면 대시보드와 문의 탭에서 접수 내역을 바로 확인할 수 있습니다.

---

## 문의

작업 중 막히는 부분이나 패키지 관련 질문은 이건희(work@firstmkt.co.kr)로 연락 주세요.
관련 문서: `ci4-admin/README.md` (상세), `배포-서버설정-필독.md` (보안 요약)
