# 서버 배포 시 반드시 막아야 할 경로 (필독)

현재 배포 방식은 웹루트에서 `git pull` 하는 구조라 **저장소의 모든 파일이 인터넷에 그대로 공개**됩니다.
2026-07-28 배포 직후 아래가 외부에서 다운로드되는 것을 확인했습니다.

| 경로 | 내용 | 위험 |
|---|---|---|
| `/ci4-admin/sql/seed.sql` | 관리자 계정 해시 | **조치 완료** — 계정 정보를 파일에서 제거함 |
| `/ci4-admin/app/**.php` | 백엔드 소스 (PHP 미설치라 텍스트로 읽힘) | 내부 로직·DB 설정 예시 노출 |
| `/admin/data/*.json` | 사이트 콘텐츠 데이터 | 구조 노출 (자격증명·개인정보는 없음) |
| `/_*.py` | 빌드 스크립트 | 구조 노출 |

> 관리자 비밀번호 해시가 담긴 파일은 이미 제거했지만, **위 경로들은 서버에서 차단하는 것이 맞습니다.**

---

## 1. nginx(openresty) 차단 규칙

사이트 설정에 아래를 추가해 주세요. 1Panel → 웹사이트 → 설정 → 구성 파일에서 편집합니다.

```nginx
# 소스·데이터 디렉터리 차단
location ~ ^/(ci4-admin|admin/data|_src)/ {
    deny all;
    return 404;
}

# 빌드 스크립트·문서·SQL 차단
location ~ \.(py|sql|md)$ {
    deny all;
    return 404;
}
```

적용 후 확인 (둘 다 404가 나와야 정상):

```bash
curl -o /dev/null -w "%{http_code}\n" http://도메인/ci4-admin/sql/seed.sql
curl -o /dev/null -w "%{http_code}\n" http://도메인/admin/data/settings.json
```

## 2. 관리자 계정 생성

`seed.sql`에서 계정 정보를 뺐으므로, 설치 시 직접 만들어 주세요.

```sql
SET @pw := '새_비밀번호';
INSERT INTO `admins`(username, password_hash, created_at)
  VALUES('admin', SHA2(@pw, 256), CURDATE());
```

## 3. HTTPS

현재 `https://`로는 접속되지 않습니다(`http://`만 응답). 1Panel에서 SSL 인증서를 발급해 주세요.
관리자 로그인은 세션 쿠키를 쓰므로 **HTTPS가 아니면 비밀번호가 평문으로 오갑니다.**

## 4. 관리자(CI4) 설치

정적 사이트는 배포되어 동작하지만 **관리자 백엔드는 아직 설치되지 않았습니다**(`/api/me` → 404).
설치 순서는 `ci4-admin/README.md`를 참고하세요. 설치 후 이 저장소의 `ci4-admin/` 디렉터리는
웹루트에서 제외하거나 위 1번 규칙으로 차단해 주세요.
